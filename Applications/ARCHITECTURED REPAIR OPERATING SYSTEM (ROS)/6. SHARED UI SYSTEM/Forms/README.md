# ROS Concern Folder

This is a real GitHub folder in the physical **ARCHITECTURED REPAIR OPERATING SYSTEM (ROS)** hierarchy.

## Required use

- Use the parent domain `INDEX.md` to identify this folder's exact concern and canonical reference baseline.
- Save future approved artifacts inside this folder or an approved child folder.
- Do not create a second copy of an existing canonical architecture merely to populate this directory.
- A controlled migration must preserve valid content, update links, pass duplicate and no-content-loss validation, and record the new canonical path.
