# Configuration bootstrap

CX-R1-001 owns only environment-variable names, classifications, safe local defaults, and
validation tests. It does not load secrets, log configuration values, define a deployment
configuration system, or establish observability; those concerns remain future-owned.
