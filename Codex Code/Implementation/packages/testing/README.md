# Bootstrap validation tools

This package owns only the deterministic CX-R1-001 release-lock, path-boundary, and
secret-pattern checks. It does not establish the later shared testing framework or fixtures.
